import ProfileLayout from "@/layout/ProfileLayout";

const Following = () => {
    return (
        <ProfileLayout>
            Following page
        </ProfileLayout>
    );
}

export default Following;